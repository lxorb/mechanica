node_modules
dist
dev-dist
.DS_Store
*.local

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# demo video masters (70-80 MB each): keep on disk, submit from disk; the webm + sheets are tracked
docs/pitches/video/*.mp4
