# Mechanica - the numbers, generated

Generated by `api/tools/pitch_numbers.py` --live on **2026-09-20 15:46**. Do not edit this file by hand;
re-run the script. Every pitch quotes from here, and where two agents measured the same word differently,
both definitions are below with the code that produced them.

```
cd api && .venv/Scripts/python tools/pitch_numbers.py --live --md ../docs/pitches/numbers.md
```

**measured** = counted or timed, from a file or a live endpoint · **computed** = arithmetic over measured values,
formula in the source column · **assumed** = nobody measured it, and the row says whose assumption it is.

## 1. The registry - what we can reach

| number | value | source |
|---|---|---|
| registry rows | **99,338** *(measured)* | `api/data/registry.json` - `len(registry.json)`, one row per (make, model, year, market, document) |
| makes in the registry | 82 *(measured)* | same - `len({row.make})` |
| portals | **90** *(measured)* | same - `len({row.site})`, the adapter's own name for the publisher portal |
| distinct hostnames serving the files | 89 *(measured)* | same - `len({hostname(row.url)})`. Equal to the portal count by coincidence, not by definition |
| distinct URLs behind those rows | 50,376 *(measured)* | same - `len({row.url})` |
| rows naming a file another row already names | **48,962** *(computed)* | `registry_rows - distinct_urls` = 99,338 - 50,376 |
| free English owner's-manual PDF rows | 24,324 *(measured)* | same - `registry._ingestable(row)`: `type=='owner' and access=='free' and lang startswith 'en' and url is a PDF` |
| distinct free English PDFs we can fetch | **14,865** *(computed)* | `len({row.url for ingestable rows})` - the honest 'manuals we can reach' number |
| service-manual rows | 186 *(measured)* | same - `row.type=='service'` |
| …of those, not free | **14** *(computed)* | `service rows - free` - {'paid': 7, 'subscription': 2, 'dealer': 5, 'free': 172} |
| portals walked to the end and written off | **17** *(measured)* | `docs/MANUALS.md` 'Checked and genuinely unreachable' - CFMOTO, LiveWire, Beta, Moto Morini, Brixton, … |
| rows explicitly retracted by a drop list | **1,449** *(measured)* | `api/data/registry-fragments/*.drop.json` - sum of the 3 drop lists; the only way a fragment un-says a row |

## 2. The catalog - what a rider can pick

| number | value | source |
|---|---|---|
| vehicles you can pick | **30,409** *(measured)* | live `GET /api/catalog` - `len(catalog)` |
| …with a free official manual attached | **18,564** *(measured)* | live `GET /api/catalog` - `count(row.manualUrl)`. NOT the same as distinct PDFs: many years share one file |
| motorcycles | 24,026 *(measured)* | live `GET /api/catalog` - `row.kind != 'car'` (motorcycles carry no kind) |
| …with a free manual | 12,357 *(measured)* | live `GET /api/catalog` - same filter plus `row.manualUrl` |
| cars | 6,383 *(measured)* | live `GET /api/catalog` - `row.kind == 'car'` |
| …with a free manual | 6,207 *(measured)* | live `GET /api/catalog` - same filter plus `row.manualUrl` |
| makes in the catalog | 79 *(measured)* | live `GET /api/catalog` - `len({row.make})`. Lower than the registry's makes: a make with no usable row never becomes a vehicle |
| …of those, an official manual NOT in English | **4,643** *(measured)* | live `GET /api/catalog` - `count(row.manualUrl and row.lang)`. `Bike.lang` is set only when no English manual exists for that vehicle, so this is the language-fallback tail of the 18,564: ja 2,664, id 404, th 348, vi 270, de 268, … Still the manufacturer's own book - but never present these as English manuals |
| …of those, a PDF we rendered from the publisher's online manual | **25** *(measured)* | live `GET /api/catalog` - `row.manualUrl` under `/pdf/rendered/`: 24 distinct files, Lexus only, where the official manual is published as a web app and no PDF exists. The manufacturer's own text, re-laid-out by us - the one place the page is not literally their file, so say so |
| catalog rows that show a photo | **69.5%** *(measured)* | `docs/qa/images-gaps.md` - 21,130 of 30,389 rows resolve through `lookupImage` in `web/counter/js/ttm.js`. Regenerate with `node web/tools/images-coverage.mjs --write`; its row total is its own catalog read, so it trails the live one by a few rows |
| …distinct models that show a photo | 55.0% *(measured)* | same - 5,035 of 9,155 distinct (make, model) pairs. Lower than the row figure because the models still missing are long-tail ones with few model years - quote the row figure for coverage, this one for how much work is left |

## 3. Manuals already indexed

| number | value | source |
|---|---|---|
| manuals indexed and searchable now | **543** *(measured)* | live `GET /api/manuals` - `len(manuals)` |
| printed pages behind them | 97,230 *(measured)* | live `GET /api/manuals` - `sum(manual.pages)` |
| sections indexed | 92,682 *(measured)* | live `GET /api/manuals` - `sum(manual.sections)` |
| vehicles those manuals cover | 670 *(measured)* | live `GET /api/manuals` - `len({id for manual.bikeIds})`; one manual covers several model years |
| median manual | 170 pages *(computed)* | live `GET /api/manuals` - `median(manual.pages)` |
| largest manual deployed | **775 pages** *(measured)* | live `GET /api/manuals` - `max(manual.pages)`; this is the page count `naivePerAsk` is computed over |

## 4. Cost

| number | value | source |
|---|---|---|
| everything ever spent building this | **$87.77** *(measured)* | `api/data/costs.jsonl` - `sum(usd)` over all 44,521 logged calls, development included |
| model calls behind it | 44,521 *(measured)* | `api/data/costs.jsonl` - `len(log)`. Grows every time anything runs; quote the date |
| …of which gpt-5.6-luna | $78.18 *(computed)* | same - `sum(usd) group by model`; 89% of the build ledger |
| …of which reading manuals once | 81% *(computed)* | same - `(ingest.struct + ingest.keywords) / total` = $71.52 / $87.77. Paid per manual, never per question |
| …of which grading catalog photos | 8,681 calls, $4.65 *(measured)* | same - `route=='images.score'`. Vision as a judge over the catalog photos; keeps growing, so quote it with the date |
| …of which generating part illustrations | 102 calls, $4.31 *(measured)* | same - `route=='illustrations'` (`gpt-image-1`) |
| what the DEPLOYED app has spent | **$13.80** *(measured)* | live `GET /api/cost` -> `total`, over `count` = 2,182 calls. In-replica; moves every hour and resets on redeploy |
| …over model calls | 2,182 *(measured)* | same -> `count` |
| naive: the whole manual in the prompt | **$12.40** / question *(measured)* | live `GET /api/cost` -> `naivePerAsk` = `llm.naive_usd(max(manual.pages))` |
| naive $/question, largest deployed manual | **$12.40** *(computed)* | `app/llm.naive_usd(775)` = 775p x 800 tok x $10/M x 2 (past the 272k long-context line) |
| naive $/question, median deployed manual | **$1.36** *(computed)* | `app/llm.naive_usd(170)` = 170p x 800 tok x $10/M, no 2x (under 272k) |
| ours: $ per ask | **$0.00038** *(measured)* | `api/eval/report.md` - cost-log delta on `ask.*` over 150 controlled queries / 150. THE number to quote |
| ours: $ per ask over the whole build log | $0.00056 *(computed)* | `costs.jsonl` - `sum(usd where route startswith 'ask') / (ask.router calls - chat.answer calls)` = $1.1798 / 2101. Development traffic, so picker-heavier than a rider's |
| …the same log, per CALL not per ask | $0.00042 *(computed)* | `costs.jsonl` - `mean(usd where route startswith 'ask')` over 2,831 calls. An ask is 1.15 calls, so this is NOT $/ask |
| ours: $ per written chat answer | **$0.0041** *(measured)* | `costs.jsonl` - `mean(usd where route=='chat.answer')` over 248 answers (the 25-question eval says $0.00488; quote either, name which) |
| manuals ingested in the bulk run | 648 *(measured)* | `api/data/mass_report.jsonl` - `count(status=='done')`; the file has 773 lines in total, the rest error / duplicate / suspicious |
| ours: $ to read a manual, once | **$0.0951** *(measured)* | `api/data/mass_report.jsonl` - `mean(usd where status=='done')` over 648 real ingests, mean 177 pages |
| …median | $0.0972 *(measured)* | same - `median(usd)`, median 170 pages |
| …per 1,000 printed pages | $0.538 *(computed)* | same - `sum(usd) / sum(pages) * 1000` |
| ingest pays for itself after | **0.07 questions** *(computed)* | `ingest_usd / naive_median` = $0.0951 / $1.36 |
| ours vs naive, per question | **32,632x cheaper** *(computed)* | `naive_max / ask_usd_eval` = $12.40 / $0.00038. Against the MEDIAN manual it is 3,579x - say which |
| …the same against the MEDIAN manual | 3,579x cheaper *(computed)* | `naive_median / ask_usd_eval` = $1.36 / $0.00038. The honest one to pair with the headline factor |

## 5. Accuracy and honesty

| number | value | source |
|---|---|---|
| right section first, in-scope questions | **100%** of 130 *(measured)* | `api/eval/report.md` - top-1 over the in-scope half of 150 queries |
| off-topic questions returned empty instead of guessed at | **20 of 20** *(measured)* | `api/eval/report.md` - the `oos` category, 100% empty |
| LLM calls per ask | 1.15 *(computed)* | `api/eval/report.md` - 172 calls / 150 asks, so 85% of asks never pay for a picker |
| chat claim sentences carrying a page number | **100%** of 48 *(measured)* | `api/eval/chat-report.md` |
| chat quotes verbatim on the page they name | **100%** of 43 *(measured)* | `api/eval/chat-report.md` - the server slices the quote out of the page, the model only names it |
| invented numbers · dealer referrals | **0 · 0** of 25 *(measured)* | `api/eval/chat-report.md` |

## 6. Speed

| number | value | source |
|---|---|---|
| an ask, mean | 1.60 s *(measured)* | `api/eval/report.md` - 150 queries, sequential, cold cache |
| an ask, p95 | **3.33 s** *(measured)* | `api/eval/report.md` - same run |
| a question, first time | **2.2 s** *(measured)* | live ask, first time, 2026-09-20 (range 2.2-3.4 s) |
| a question, asked again | **0.2 s** *(measured)* | live ask, same question again, 2026-09-20 - served from ask._cache, $0 |
| VIN -> the exact bike | **1.3 s** *(measured)* | live POST /api/identify/vin VBKJSA40XXXXXXXXX -> KTM 390 Duke 2024, 2026-09-20 |
| a brand-new manual -> readable | **1.3 s** *(measured)* | docs/pitches/general.md - live run, KTM 390 Duke 2014, 182 p, 2026-09-20 |
| a brand-new manual -> fully searchable | **40.4 s** *(measured)* | same run: /ingest job to status=done |
| …what that one run cost | **$0.089** *(measured)* | same run: cost-log delta over the run |
| chat, first word | 2.64 s p50 / 5.87 s p95 *(measured)* | `api/eval/chat-report.md` |
| voice (Deepgram Agent): you stop talking -> it starts | **2.08 s** median *(measured)* | docs/pitches/deepgram.md - 5 spec turns, 4 sessions, 2026-09-20; range 1.73-4.01 s. A procedure question is 9.9 s - say so |
| voice (shipped engine) | 1.5-2.0 s *(measured)* | web/docs/VOICE.md - end of speech to first audio on the shipped engine |

## 7. The Token Company

| number | value | source |
|---|---|---|
| tokens removed before the prompt was billed | **31.2%** *(measured)* | `api/eval/chat-report.md` - 46,501 tok in -> 31,977 out over 23 bear-2 calls |
| …the same counter on the live replica right now | 0.0% over 0 calls *(measured)* | live `GET /api/cost/ttc` -> `total.savedPct`. In-memory PER REPLICA and zeroed on restart, so a low number means a fresh replica, not a worse compressor. Quote the eval figure on stage |

## 8. Parts

| number | value | source |
|---|---|---|
| parts for the demo bike (KTM 390 Duke 2024) | **135** *(measured)* | live `GET /api/parts/catalog?manualId=ktm-390-duke-2024-om-en` - `len(parts)`, in 11 groups; 14 of them carry a spec this manual itself prints, the rest come from the parts taxonomy |

## 9. The founder's friend - HIS numbers, not ours

| number | value | source |
|---|---|---|
| share of his working time spent looking for a page | **20%** *(assumed)* | HIS estimate, reported by the founder. Not a study, not measured by us. Always say 'he says' |
| what one question cost him when he tried an LLM | **~$4** *(assumed)* | HIS bill. Our own worst case on the deployed catalog is the $12.40 above; the two are not the same measurement |
| hours a year | **400 h** *(computed)* | `20% x 2,000 h` - arithmetic on his number and an assumed 2,000-hour year (`docs/pitches/ramp/numbers.py`) |
| what those hours bill out at, Germany | **EUR 36k-48k** *(computed)* | `400 h x EUR 90-120/h`. `docs/pitches/ramp/numbers.md` defends EUR 24,640 after recovery and utilisation - prefer that on stage |

## 10. Two definitions of the same word

| number | value | source |
|---|---|---|
| mislabelled rows (owner-typed) | **6,244** *(measured)* | `api/data/registry.json` - `row.type=='owner' and doctype.classify_doc(url, title) != 'owner'`, over ALL rows in every language and access level |
| mislabelled rows (free-English) | **1,507** *(measured)* | same file - `access=='free' and lang=='en' and classify_doc(...) != 'owner'`. A SUBSET question: of the rows we would actually try to fetch, how many are not a handbook |

## 11. The same word, two honest counts

Three agents disagreed on these. Nobody was wrong; they counted different things. Pick one and say its definition.

| the word | definition A | definition B | which is which |
|---|---|---|---|
| mislabelled rows | **6,244** owner-typed | **1,507** free-English | 6,244 = the portal filed it as an owner's manual and `doctype.py` says otherwise, counted over every row in every language (infotainment 3,632, supplement 1,767, quickstart 572, spec 183…). 1,507 = the same test restricted to free English rows - the ones we would actually fetch. Dropbox's slide uses the first; the brief's old 1,254 is the lineage of the second. Say which. |
| manuals we can reach | **14,865** distinct PDFs | **18,564** vehicles | 14,865 = distinct URLs behind the free-English-owner-PDF rows. 18,564 = catalog rows carrying a `manualUrl`; many model years point at one file, and many fetchable PDFs belong to no catalog vehicle. Never call the vehicle count 'manuals'. And of those 18,564, 4,643 carry an official manual in another language (no English one exists for that vehicle) and 25 carry a PDF we rendered from the publisher's online manual - both are the manufacturer's own book, neither is an English PDF. |
| $ per question, naive | **$12.40** worst case | **$1.36** median | $12.40 = `naive_usd(largest deployed manual)`, which is past the 272k-token line and therefore billed 2x - this is what live `GET /api/cost` reports as `naivePerAsk`. $1.36 = the same formula on the median manual. The founder's friend's ~$4 is HIS bill on HIS manual and is a third number. |
| $ per ask, ours | **$0.00038** eval | **$0.00056** build log | The eval figure is 150 controlled rider queries and is the one to quote. The build-log figure divides every `ask.*` dollar by the asks in the log, which is development traffic and therefore picker-heavy. Both are honest; they answer different questions. |
| tokens saved by compression | **31.2%** eval | **0.0% over 0 calls** live | The eval figure is 23 bear-2 calls over a fixed 25-question set. The live counter is in-memory PER REPLICA and zeroed on restart, so it reports whatever that replica has done since it booted. Quote the eval figure. |
| total spent | **$87.77** build | **$13.80** live | The build ledger is every call ever made on the dev machine - ingest, image generation, photo grading, the lot. The live ledger is only the deployed replica since its last restart. The pitch-friendly 'it cost us six dollars to run' is the live one, and it moves every hour: quote it with a date or do not quote a total at all. |

## 12. Discrepancies in the documents that quote these numbers

Every figure that contradicts the tables above, found by `pitch_numbers.py`'s own grep (`CHECKS` in the
script). **Nothing here has been edited** - this is the work list for the fix pass. Add a pattern to
`CHECKS` when a new stale figure appears, so the list stays generated rather than typed.

Scanned: the nine pitches, plus `docs/pitches/BRIEF.md`, `docs/pitches/README.md`, `docs/PITCH.md`, `docs/DEMO.md`, `docs/SUBMISSION.md`, `docs/ARCHITECTURE.md`, `docs/MANUALS.md`, `docs/codex/run-3.md`, `docs/pitches/ramp/numbers.md`, `docs/pitches/token-company/cost-report.md`.
A line that names a figure in order to FORBID it (`Do not say $6.096`, `superseded`) is skipped - the
do-not-say lists in `docs/pitches/README.md` are the point, not a defect.

### `docs/PITCH.md` - 1 figure(s)

| line | the pitch says | should be | why |
|---|---|---|---|
| `docs/PITCH.md:128` | 15 service rows not free | **14** | pre-merge crawl state |

### `docs/SUBMISSION.md` - 1 figure(s)

| line | the pitch says | should be | why |
|---|---|---|---|
| `docs/SUBMISSION.md:61` | 15 service rows not free | **14** | pre-merge crawl state |

### `docs/pitches/token-company/cost-report.md` - 5 figure(s)

| line | the pitch says | should be | why |
|---|---|---|---|
| `docs/pitches/token-company/cost-report.md:17` | $8.08 / $8.93 live ledger | **$13.80** | the live ledger moves every hour; quote it with a timestamp or not at all |
| `docs/pitches/token-company/cost-report.md:62` | median manual 171 pages | **170 pages** | the median deployed manual is 170 pages now |
| `docs/pitches/token-company/cost-report.md:124` | $1.37 naive median | **$1.36** | the median deployed manual is a page shorter now |
| `docs/pitches/token-company/cost-report.md:129` | median manual 171 pages | **170 pages** | the median deployed manual is 170 pages now |
| `docs/pitches/token-company/cost-report.md:150` | 535 manuals | **543** | more have been ingested since |

### `docs/pitches/voloridge.md` - 1 figure(s)

| line | the pitch says | should be | why |
|---|---|---|---|
| `docs/pitches/voloridge.md:197` | 4,113 cars with a manual | **6,207** | pre-freeze catalog |

**8 contradicting figures across 4 files.**

