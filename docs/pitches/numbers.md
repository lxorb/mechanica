# Mechanica - the numbers, generated

Generated by `api/tools/pitch_numbers.py` --live on **2026-09-20 10:04**. Do not edit this file by hand;
re-run the script. Every pitch quotes from here, and where two agents measured the same word differently,
both definitions are below with the code that produced them.

```
cd api && .venv/Scripts/python tools/pitch_numbers.py --live --md ../docs/pitches/numbers.md
```

**measured** = counted or timed, from a file or a live endpoint · **computed** = arithmetic over measured values,
formula in the source column · **assumed** = nobody measured it, and the row says whose assumption it is.

## 1. The registry - what we can reach

| number | value | source |
|---|---|---|
| registry rows | **53,557** *(measured)* | `api/data/registry.json` - `len(registry.json)`, one row per (make, model, year, market, document) |
| makes in the registry | 80 *(measured)* | same - `len({row.make})` |
| portals | **84** *(measured)* | same - `len({row.site})`, the adapter's own name for the publisher portal |
| distinct hostnames serving the files | 84 *(measured)* | same - `len({hostname(row.url)})`. Equal to the portal count by coincidence, not by definition |
| distinct URLs behind those rows | 28,200 *(measured)* | same - `len({row.url})` |
| rows naming a file another row already names | **25,357** *(computed)* | `registry_rows - distinct_urls` = 53,557 - 28,200 |
| free English owner's-manual PDF rows | 24,210 *(measured)* | same - `registry._ingestable(row)`: `type=='owner' and access=='free' and lang startswith 'en' and url is a PDF` |
| distinct free English PDFs we can fetch | **14,770** *(computed)* | `len({row.url for ingestable rows})` - the honest 'manuals we can reach' number |
| service-manual rows | 182 *(measured)* | same - `row.type=='service'` |
| …of those, not free | **15** *(computed)* | `service rows - free` - {'paid': 7, 'subscription': 3, 'dealer': 5, 'free': 167} |
| portals walked to the end and written off | **17** *(measured)* | `docs/MANUALS.md` 'Checked and genuinely unreachable' - CFMOTO, LiveWire, Beta, Moto Morini, Brixton, … |
| rows explicitly retracted by a drop list | **1,444** *(measured)* | `api/data/registry-fragments/*.drop.json` - sum of the 2 drop lists; the only way a fragment un-says a row |

## 2. The catalog - what a rider can pick

| number | value | source |
|---|---|---|
| vehicles you can pick | **27,751** *(measured)* | live `GET /api/catalog` - `len(catalog)` |
| …with a free official manual attached | **13,537** *(measured)* | live `GET /api/catalog` - `count(row.manualUrl)`. NOT the same as distinct PDFs: many years share one file |
| motorcycles | 23,140 *(measured)* | live `GET /api/catalog` - `row.kind != 'car'` (motorcycles carry no kind) |
| …with a free manual | 9,424 *(measured)* | live `GET /api/catalog` - same filter plus `row.manualUrl` |
| cars | 4,611 *(measured)* | live `GET /api/catalog` - `row.kind == 'car'` |
| …with a free manual | 4,113 *(measured)* | live `GET /api/catalog` - same filter plus `row.manualUrl` |
| makes in the catalog | 77 *(measured)* | live `GET /api/catalog` - `len({row.make})`. Lower than the registry's makes: a make with no usable row never becomes a vehicle |

## 3. Manuals already indexed

| number | value | source |
|---|---|---|
| manuals indexed and searchable now | **535** *(measured)* | live `GET /api/manuals` - `len(manuals)` |
| printed pages behind them | 95,914 *(measured)* | live `GET /api/manuals` - `sum(manual.pages)` |
| sections indexed | 91,388 *(measured)* | live `GET /api/manuals` - `sum(manual.sections)` |
| vehicles those manuals cover | 662 *(measured)* | live `GET /api/manuals` - `len({id for manual.bikeIds})`; one manual covers several model years |
| median manual | 171 pages *(computed)* | live `GET /api/manuals` - `median(manual.pages)` |
| largest manual deployed | **775 pages** *(measured)* | live `GET /api/manuals` - `max(manual.pages)`; this is the page count `naivePerAsk` is computed over |

## 4. Cost

| number | value | source |
|---|---|---|
| everything ever spent building this | **$83.21** *(measured)* | `api/data/costs.jsonl` - `sum(usd)` over all 41,918 logged calls, development included |
| model calls behind it | 41,918 *(measured)* | `api/data/costs.jsonl` - `len(log)`. Grows every time anything runs; quote the date |
| …of which gpt-5.6-luna | $76.03 *(computed)* | same - `sum(usd) group by model`; 91% of the build ledger |
| …of which reading manuals once | 86% *(computed)* | same - `(ingest.struct + ingest.keywords) / total` = $71.52 / $83.21. Paid per manual, never per question |
| …of which grading catalog photos | 7,611 calls, $3.65 *(measured)* | same - `route=='images.score'`. Vision as a judge over the catalog photos; keeps growing, so quote it with the date |
| …of which generating part illustrations | 102 calls, $4.31 *(measured)* | same - `route=='illustrations'` (`gpt-image-1`) |
| what the DEPLOYED app has spent | **$8.93** *(measured)* | live `GET /api/cost` -> `total`, over `count` = 1,633 calls. In-replica; moves every hour and resets on redeploy |
| …over model calls | 1,633 *(measured)* | same -> `count` |
| naive: the whole manual in the prompt | **$12.40** / question *(measured)* | live `GET /api/cost` -> `naivePerAsk` = `llm.naive_usd(max(manual.pages))` |
| naive $/question, largest deployed manual | **$12.40** *(computed)* | `app/llm.naive_usd(775)` = 775p x 800 tok x $10/M x 2 (past the 272k long-context line) |
| naive $/question, median deployed manual | **$1.37** *(computed)* | `app/llm.naive_usd(171)` = 171p x 800 tok x $10/M, no 2x (under 272k) |
| ours: $ per ask | **$0.00038** *(measured)* | `api/eval/report.md` - cost-log delta on `ask.*` over 150 controlled queries / 150. THE number to quote |
| ours: $ per ask over the whole build log | $0.00078 *(computed)* | `costs.jsonl` - `sum(usd where route startswith 'ask') / (ask.router calls - chat.answer calls)` = $0.8120 / 1035. Development traffic, so picker-heavier than a rider's |
| …the same log, per CALL not per ask | $0.00052 *(computed)* | `costs.jsonl` - `mean(usd where route startswith 'ask')` over 1,573 calls. An ask is 1.15 calls, so this is NOT $/ask |
| ours: $ per written chat answer | **$0.0041** *(measured)* | `costs.jsonl` - `mean(usd where route=='chat.answer')` over 248 answers (the 25-question eval says $0.00488; quote either, name which) |
| manuals ingested in the bulk run | 648 *(measured)* | `api/data/mass_report.jsonl` - `count(status=='done')`; the file has 773 lines in total, the rest error / duplicate / suspicious |
| ours: $ to read a manual, once | **$0.0951** *(measured)* | `api/data/mass_report.jsonl` - `mean(usd where status=='done')` over 648 real ingests, mean 177 pages |
| …median | $0.0972 *(measured)* | same - `median(usd)`, median 170 pages |
| …per 1,000 printed pages | $0.538 *(computed)* | same - `sum(usd) / sum(pages) * 1000` |
| ingest pays for itself after | **0.07 questions** *(computed)* | `ingest_usd / naive_median` = $0.0951 / $1.37 |
| ours vs naive, per question | **32,632x cheaper** *(computed)* | `naive_max / ask_usd_eval` = $12.40 / $0.00038. Against the MEDIAN manual it is 3,600x - say which |

## 5. Accuracy and honesty

| number | value | source |
|---|---|---|
| right section first, in-scope questions | **100%** of 130 *(measured)* | `api/eval/report.md` - top-1 over the in-scope half of 150 queries |
| off-topic questions returned empty instead of guessed at | **20 of 20** *(measured)* | `api/eval/report.md` - the `oos` category, 100% empty |
| LLM calls per ask | 1.15 *(computed)* | `api/eval/report.md` - 172 calls / 150 asks, so 85% of asks never pay for a picker |
| chat claim sentences carrying a page number | **100%** of 48 *(measured)* | `api/eval/chat-report.md` |
| chat quotes verbatim on the page they name | **100%** of 43 *(measured)* | `api/eval/chat-report.md` - the server slices the quote out of the page, the model only names it |
| invented numbers · dealer referrals | **0 · 0** of 25 *(measured)* | `api/eval/chat-report.md` |

## 6. Speed

| number | value | source |
|---|---|---|
| an ask, mean | 1.60 s *(measured)* | `api/eval/report.md` - 150 queries, sequential, cold cache |
| an ask, p95 | **3.33 s** *(measured)* | `api/eval/report.md` - same run |
| a question, first time | **2.2 s** *(measured)* | live ask, first time, 2026-09-20 (range 2.2-3.4 s) |
| a question, asked again | **0.2 s** *(measured)* | live ask, same question again, 2026-09-20 - served from ask._cache, $0 |
| VIN -> the exact bike | **1.3 s** *(measured)* | live POST /api/identify/vin VBKJSA40XXXXXXXXX -> KTM 390 Duke 2024, 2026-09-20 |
| a brand-new manual -> readable | **1.3 s** *(measured)* | docs/pitches/general.md - live run, KTM 390 Duke 2014, 182 p, 2026-09-20 |
| a brand-new manual -> fully searchable | **40.4 s** *(measured)* | same run: /ingest job to status=done |
| …what that one run cost | **$0.089** *(measured)* | same run: cost-log delta over the run |
| chat, first word | 2.64 s p50 / 5.87 s p95 *(measured)* | `api/eval/chat-report.md` |
| voice (Deepgram Agent): you stop talking -> it starts | **2.08 s** median *(measured)* | docs/pitches/deepgram.md - 5 spec turns, 4 sessions, 2026-09-20; range 1.73-4.01 s. A procedure question is 9.9 s - say so |
| voice (shipped engine) | 1.5-2.0 s *(measured)* | web/docs/VOICE.md - end of speech to first audio on the shipped engine |

## 7. The Token Company

| number | value | source |
|---|---|---|
| tokens removed before the prompt was billed | **31.2%** *(measured)* | `api/eval/chat-report.md` - 46,501 tok in -> 31,977 out over 23 bear-2 calls |
| …the same counter on the live replica right now | 0.0% over 0 calls *(measured)* | live `GET /api/cost/ttc` -> `total.savedPct`. In-memory PER REPLICA and zeroed on restart, so a low number means a fresh replica, not a worse compressor. Quote the eval figure on stage |

## 8. Parts

| number | value | source |
|---|---|---|
| parts for the demo bike (KTM 390 Duke 2024) | **135** *(measured)* | live `GET /api/parts/catalog?manualId=ktm-390-duke-2024-om-en` - `len(parts)`, in 11 groups; 14 of them carry a spec this manual itself prints, the rest come from the parts taxonomy |

## 9. The founder's friend - HIS numbers, not ours

| number | value | source |
|---|---|---|
| share of his working time spent looking for a page | **20%** *(assumed)* | HIS estimate, reported by the founder. Not a study, not measured by us. Always say 'he says' |
| what one question cost him when he tried an LLM | **~$4** *(assumed)* | HIS bill. Our own worst case on the deployed catalog is the $12.40 above; the two are not the same measurement |
| hours a year | **400 h** *(computed)* | `20% x 2,000 h` - arithmetic on his number and an assumed 2,000-hour year (`docs/pitches/ramp/numbers.py`) |
| what those hours bill out at, Germany | **EUR 36k-48k** *(computed)* | `400 h x EUR 90-120/h`. `docs/pitches/ramp/numbers.md` defends EUR 24,640 after recovery and utilisation - prefer that on stage |

## 10. Two definitions of the same word

| number | value | source |
|---|---|---|
| mislabelled rows (owner-typed) | **4,081** *(measured)* | `api/data/registry.json` - `row.type=='owner' and doctype.classify_doc(url, title) != 'owner'`, over ALL rows in every language and access level |
| mislabelled rows (free-English) | **1,489** *(measured)* | same file - `access=='free' and lang=='en' and classify_doc(...) != 'owner'`. A SUBSET question: of the rows we would actually try to fetch, how many are not a handbook |

## 11. The same word, two honest counts

Three agents disagreed on these. Nobody was wrong; they counted different things. Pick one and say its definition.

| the word | definition A | definition B | which is which |
|---|---|---|---|
| mislabelled rows | **4,081** owner-typed | **1,489** free-English | 4,081 = the portal filed it as an owner's manual and `doctype.py` says otherwise, counted over every row in every language (infotainment 2,793, supplement 578, quickstart 479, spec 159…). 1,489 = the same test restricted to free English rows - the ones we would actually fetch. Dropbox's slide uses the first; the brief's old 1,254 is the lineage of the second. Say which. |
| manuals we can reach | **14,770** distinct PDFs | **13,537** vehicles | 14,770 = distinct URLs behind the free-English-owner-PDF rows. 13,537 = catalog rows carrying a `manualUrl`; many model years point at one file, and many fetchable PDFs belong to no catalog vehicle. Never call the vehicle count 'manuals'. |
| $ per question, naive | **$12.40** worst case | **$1.37** median | $12.40 = `naive_usd(largest deployed manual)`, which is past the 272k-token line and therefore billed 2x - this is what live `GET /api/cost` reports as `naivePerAsk`. $1.37 = the same formula on the median manual. The founder's friend's ~$4 is HIS bill on HIS manual and is a third number. |
| $ per ask, ours | **$0.00038** eval | **$0.00078** build log | The eval figure is 150 controlled rider queries and is the one to quote. The build-log figure divides every `ask.*` dollar by the asks in the log, which is development traffic and therefore picker-heavy. Both are honest; they answer different questions. |
| tokens saved by compression | **31.2%** eval | **0.0% over 0 calls** live | The eval figure is 23 bear-2 calls over a fixed 25-question set. The live counter is in-memory PER REPLICA and zeroed on restart, so it reports whatever that replica has done since it booted. Quote the eval figure. |
| total spent | **$83.21** build | **$8.93** live | The build ledger is every call ever made on the dev machine - ingest, image generation, photo grading, the lot. The live ledger is only the deployed replica since its last restart. The pitch-friendly 'it cost us six dollars to run' is the live one, and it moves every hour: quote it with a date or do not quote a total at all. |

## 12. Discrepancies in the pitch files

Every figure in `docs/pitches/*.md` that contradicts the tables above, found by `pitch_numbers.py`'s own
grep (`CHECKS` in the script). **Nothing here has been edited** - this is the work list for the fix pass.
Add a pattern to `CHECKS` when a new stale figure appears, so the list stays generated rather than typed.

### `docs/ARCHITECTURE.md` - 5 figure(s)

| line | the pitch says | should be | why |
|---|---|---|---|
| `docs/ARCHITECTURE.md:18` | 22,300 bikes | **23,140** | the catalog grew |
| `docs/ARCHITECTURE.md:35` | 319 ingest runs | **648** | `mass_report.jsonl` now carries this many runs with `status=='done'` |
| `docs/ARCHITECTURE.md:42` | $6.096 naive baseline | **$12.40** | the largest deployed manual grew past the 272k long-context line |
| `docs/ARCHITECTURE.md:43` | $1.172 live ledger | **$8.93** | same |
| `docs/ARCHITECTURE.md:43` | 569 live calls | **1,633** | same |

### `docs/MANUALS.md` - 3 figure(s)

| line | the pitch says | should be | why |
|---|---|---|---|
| `docs/MANUALS.md:3` | 39,119 registry rows | **53,557** | pre-merge crawl state |
| `docs/MANUALS.md:9` | 39,119 registry rows | **53,557** | pre-merge crawl state |
| `docs/MANUALS.md:12` | 8,319 free official PDFs | **14,770** | pre-merge crawl state |

### `docs/codex/run-3.md` - 1 figure(s)

| line | the pitch says | should be | why |
|---|---|---|---|
| `docs/codex/run-3.md:65` | $6.096 naive baseline | **$12.40** | the largest deployed manual grew past the 272k long-context line |

### `docs/pitches/README.md` - 8 figure(s)

| line | the pitch says | should be | why |
|---|---|---|---|
| `docs/pitches/README.md:82` | 17,556 distinct free English PDFs | **14,770** | no definition in the repo yields 17,556; the ingestable set is this many distinct URLs |
| `docs/pitches/README.md:92` | $6.096 naive baseline | **$12.40** | the largest deployed manual grew past the 272k long-context line |
| `docs/pitches/README.md:92` | $6.10 naive baseline | **$12.40** | same |
| `docs/pitches/README.md:92` | 8,319 free official PDFs | **14,770** | pre-merge crawl state |
| `docs/pitches/README.md:92` | largest manual 762 pages | **775 pages** | a larger manual has been ingested since |
| `docs/pitches/README.md:93` | $79.91 build ledger | **$83.21** | same |
| `docs/pitches/README.md:93` | 36,620 logged calls | **41,918** | the build ledger grows every time anything runs |
| `docs/pitches/README.md:99` | 31.4% of tokens saved, live | **0.0% over 0 calls** | the live TTC counter is in-memory per replica and resets on restart; only the eval figure is stable |

**17 contradicting figures across 4 files.**

