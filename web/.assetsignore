docs-shots/
tools/
# Raw Sketchfab downloads only: scene.bin is 26.5 MiB and Workers caps one asset file at 25 MiB.
# The shipped model.glb (1.5-4.2 MiB, Draco), license.txt, CREDITS.md and env/ HDRIs must stay in
# the deploy — the Pick screen's 3D viewer loads them at runtime. Owner: 3d agent.
store/models/*/scene.*
store/models/*/textures/
store/icons-parts-3d/*.png
store/icons-parts-3d/sheet.png
